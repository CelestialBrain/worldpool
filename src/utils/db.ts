// ─── SQLite Connection ────────────────────────────────────────────────────────
// Lazy singleton. Applies WAL mode and runs migrations on first access.

// TODO: implement getDb(), runMigrations()

export {};
