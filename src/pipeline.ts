// ─── Pipeline CLI Entry Point ─────────────────────────────────────────────────
// Called by `npm run pipeline` and GitHub Actions.
// Runs the full pipeline and exits.

// TODO: implement CLI entry

export {};
