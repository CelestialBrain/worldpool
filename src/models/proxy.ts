// ─── Proxy Model (DAL) ────────────────────────────────────────────────────────
// All SQLite queries for the `proxy` table live here.
// No raw SQL in routes — always go through this module.

// TODO: implement upsertProxy(), queryProxy(), getRandomProxy(), getStats()

export {};
